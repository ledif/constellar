pre-commit:
  commands:
    clang-format:
      glob: "*.{cpp,h}"
      run: just format {staged_files}
      stage_fixed: true
    cmake-format:
      glob: "{*.cmake,*CMakeLists.txt}"
      run: just format {staged_files}
      stage_fixed: true
